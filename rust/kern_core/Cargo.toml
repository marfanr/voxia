[package]
name = "kern_core"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["staticlib"]
test = false
bench = false

[dependencies]
"phys_base_allocator" = { path = "../phys_base_allocator", version = "0.1.0" }
"kern_utils" = { path = "../kern_utils", version = "*" }

[features]
debug = []
default = ["debug"]
