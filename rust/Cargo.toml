[workspace]
members = ["kern_core", "kern_types", "kern_utils", "phys_base_allocator"]

resolver = "2"

[workspace.package]
edition = "2021"
license = "MIT"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
